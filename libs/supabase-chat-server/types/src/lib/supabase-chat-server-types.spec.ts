import { supabaseChatServerTypes } from './supabase-chat-server-types';

describe('supabaseChatServerTypes', () => {
  it('should work', () => {
    expect(supabaseChatServerTypes()).toEqual('supabase-chat-server-types');
  });
});
